* - nije pregledano - *
SET CONFIRM ON
SET DATE GERMAN
SET MESSAGE TO 24 CENTER
PUBLIC DATUM,V
DATUM = DATE()
USE RASHODN INDEX RASHINKN,RASHINDN
B = LASTREC()
B = B + 1
DO WHILE .T.
   CLEAR SCREEN
   V = 0
   DN = " "
   WRRB = VAL("0000")
   WRRB = RRB
   WRDK = RDK
   WROPIS = ROPIS
   WRBR = RBR
   WRDATDOK = RDATDOK
   WRPV = RPV
   WRPN = RPN
   WRPN1 = RPN1
   WRPBEZ = RPBEZ
   WRUSP = RUSP
   WRUSBEZP = RUSBEZP
   WRPLACP = RPLACP
   WRSVEP = RSVEP
   WRNVRRR = RNVRRR
   WRVSP = RVSP
   WRRUCP = RRUCP
   WROBPOR = ROBPOR
   WRPRODV = RPRODV
   WRMIN = RMIN
   WRDOP = RDOP
   WRRASF = RRASF
   WRRASUK = RRASUK
   WRZIRU = RZIRU
   WRZIRI = RZIRI
   WRSK = RSK
   DO RASMLISN
   IF V = 1
     EXIT
   ENDIF
   @ 23,24 SAY "SLEDECA STAVKA  D/N  ? " GET DN PICTURE "!" VALID(DN $ "DN")
   READ
   IF Lastkey() = 27 .OR. DN = "N"
      EXIT
   ENDIF
   IF DN = "D"
      SKIP
      LOOP
   ENDIF
ENDDO
*
USE
*CLEAR ALL
CLEAR SCREEN
RETURN


